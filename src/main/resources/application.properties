spring.application.name=RewardProject 
# Data Source Configuration

server.port=8081
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver

ALTER TABLE CUSTOMERTRANSACTIONS
DROP CONSTRAINT CONSTRAINT_45;

CREATE TABLE customer_points
(
id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
customer_id BIGINT,
points      INT,
"month"     INT,
"year"      INT,
CONSTRAINT pk_customerpoints PRIMARY KEY (id)
);

ALTER TABLE customer_points
ADD CONSTRAINT FK_CUSTOMERPOINTS_ON_CUSTOMER FOREIGN KEY (customer_id) REFERENCES customer (id);

DROP TABLE CUSTOMERS CASCADE;

DROP TABLE CUSTOMERTRANSACTIONS CASCADE;